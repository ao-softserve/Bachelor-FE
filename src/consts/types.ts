export interface Delivery {
  name: string;
  qty: number;
  deliveryTime: number;
  deliveryTimeLeft: number;
}

export interface Shipment {
  name: string;
  qty: number;
  shipmentTime: number;
  shipmentTimeLeft: number;
}
